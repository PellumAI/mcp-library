schema_version: 1
name: fixture-count
version: 1.0.0
runtime: python@3.12
arch: [amd64]
# No source block: mcplib fixture supplies it, as for fixture-echo.
build:
  lockfile: requirements.txt
  steps:
    - ["python3", "-m", "pip", "install", "--require-hashes", "--no-deps", "--no-compile", "--target", ".venv/lib/python3.12/site-packages", "-r", "requirements.txt"]
  stage:
    - { from: lib, to: lib }
    - { from: .venv, to: .venv }
    - { from: LICENSE, to: LICENSE }
vetting:
  vetted_on: "2026-09-23"
  vetted_by: "the fixture generator"
  sources:
    - https://github.com/PellumAI/mcp-library/tree/main/internal/fixture
    - https://pypi.org/project/six/1.17.0/
  license: Apache-2.0
  egress_rationale: "Dials nothing."
