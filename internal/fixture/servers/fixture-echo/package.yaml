schema_version: 1
name: fixture-echo
version: 1.0.0
runtime: native
arch: [amd64]
# No source block: mcplib fixture supplies it -- kind git, this repository,
# at the checkout's own HEAD -- and copies the tree from src/ beside this file,
# so a fixture needs no upstream.
build:
  steps:
    - ["go", "build", "-trimpath", "-ldflags", "-s -w -buildid=", "-o", "out/fixture-echo", "."]
  stage:
    - { from: out/fixture-echo, to: bin/fixture-echo }
    - { from: LICENSE, to: LICENSE }
vetting:
  vetted_on: "2026-09-23"
  vetted_by: "the fixture generator"
  sources:
    - https://github.com/PellumAI/mcp-library/tree/main/internal/fixture
  license: Apache-2.0
  egress_rationale: "Dials nothing."
