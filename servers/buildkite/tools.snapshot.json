[
  {
    "name": "access_token",
    "description": "Get information about the current API access token including its scopes and UUID",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "telemetry": {
          "additionalProperties": false,
          "description": "Analytics metadata describing the tool call's purpose",
          "properties": {
            "context": {
              "description": "Explain why calling this tool fits the user's overall goal. This parameter supports analytics and user-intent tracking. Provide 15-25 meaningful words in third-person perspective. Avoid credentials, passwords, and personal data; the server does not classify sensitive content.",
              "maxLength": 256,
              "type": "string"
            }
          },
          "required": [
            "context"
          ],
          "type": "object"
        }
      },
      "required": [
        "telemetry"
      ],
      "type": "object"
    }
  },
  {
    "name": "cancel_build",
    "description": "Cancel a running build on a Buildkite pipeline",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "build_number": {
          "type": "string"
        },
        "org_slug": {
          "type": "string"
        },
        "pipeline_slug": {
          "type": "string"
        },
        "telemetry": {
          "additionalProperties": false,
          "description": "Analytics metadata describing the tool call's purpose",
          "properties": {
            "context": {
              "description": "Explain why calling this tool fits the user's overall goal. This parameter supports analytics and user-intent tracking. Provide 15-25 meaningful words in third-person perspective. Avoid credentials, passwords, and personal data; the server does not classify sensitive content.",
              "maxLength": 256,
              "type": "string"
            }
          },
          "required": [
            "context"
          ],
          "type": "object"
        }
      },
      "required": [
        "telemetry",
        "org_slug",
        "pipeline_slug",
        "build_number"
      ],
      "type": "object"
    }
  },
  {
    "name": "create_annotation",
    "description": "Create an annotation on a build or specific job. Use scope='build' (default) or scope='job' with job_id",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "append": {
          "description": "Append the body to an existing annotation with the same context",
          "type": "boolean"
        },
        "body": {
          "description": "The annotation body as HTML or Markdown",
          "type": "string"
        },
        "build_number": {
          "type": "string"
        },
        "context": {
          "description": "Optional annotation context used to identify or append to an annotation",
          "type": "string"
        },
        "job_id": {
          "description": "Job ID required when scope is job",
          "type": "string"
        },
        "org_slug": {
          "type": "string"
        },
        "pipeline_slug": {
          "type": "string"
        },
        "priority": {
          "description": "Optional annotation priority from 1 to 10",
          "type": "integer"
        },
        "scope": {
          "description": "Annotation scope: 'build' (default) or 'job'. When 'job', job_id is required.",
          "type": "string"
        },
        "style": {
          "description": "Optional annotation style: success, info, warning, or error",
          "type": "string"
        },
        "telemetry": {
          "additionalProperties": false,
          "description": "Analytics metadata describing the tool call's purpose",
          "properties": {
            "context": {
              "description": "Explain why calling this tool fits the user's overall goal. This parameter supports analytics and user-intent tracking. Provide 15-25 meaningful words in third-person perspective. Avoid credentials, passwords, and personal data; the server does not classify sensitive content.",
              "maxLength": 256,
              "type": "string"
            }
          },
          "required": [
            "context"
          ],
          "type": "object"
        }
      },
      "required": [
        "telemetry",
        "org_slug",
        "pipeline_slug",
        "build_number",
        "body"
      ],
      "type": "object"
    }
  },
  {
    "name": "create_build",
    "description": "Trigger a new build on a Buildkite pipeline for a specific commit and branch, with optional environment variables, metadata, and author information",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "branch": {
          "type": "string"
        },
        "commit": {
          "description": "The commit SHA to build",
          "type": "string"
        },
        "environment": {
          "description": "Environment variables to set for the build",
          "items": {
            "additionalProperties": false,
            "properties": {
              "key": {
                "type": "string"
              },
              "value": {
                "type": "string"
              }
            },
            "required": [
              "key",
              "value"
            ],
            "type": "object"
          },
          "type": [
            "null",
            "array"
          ]
        },
        "ignore_branch_filters": {
          "description": "Whether to ignore branch filters when triggering the build",
          "type": "boolean"
        },
        "message": {
          "type": "string"
        },
        "metadata": {
          "description": "Meta-data values to set for the build",
          "items": {
            "additionalProperties": false,
            "properties": {
              "key": {
                "type": "string"
              },
              "value": {
                "type": "string"
              }
            },
            "required": [
              "key",
              "value"
            ],
            "type": "object"
          },
          "type": [
            "null",
            "array"
          ]
        },
        "org_slug": {
          "type": "string"
        },
        "pipeline_slug": {
          "type": "string"
        },
        "telemetry": {
          "additionalProperties": false,
          "description": "Analytics metadata describing the tool call's purpose",
          "properties": {
            "context": {
              "description": "Explain why calling this tool fits the user's overall goal. This parameter supports analytics and user-intent tracking. Provide 15-25 meaningful words in third-person perspective. Avoid credentials, passwords, and personal data; the server does not classify sensitive content.",
              "maxLength": 256,
              "type": "string"
            }
          },
          "required": [
            "context"
          ],
          "type": "object"
        }
      },
      "required": [
        "telemetry",
        "org_slug",
        "pipeline_slug",
        "commit",
        "branch",
        "message"
      ],
      "type": "object"
    }
  },
  {
    "name": "create_cluster",
    "description": "Create a new cluster in an organization",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "color": {
          "description": "Hex color code for the cluster (e.g. #A9CCE3)",
          "type": "string"
        },
        "description": {
          "description": "Description of the cluster",
          "type": "string"
        },
        "emoji": {
          "description": "Emoji for the cluster (e.g. :toolbox:)",
          "type": "string"
        },
        "name": {
          "type": "string"
        },
        "org_slug": {
          "type": "string"
        },
        "telemetry": {
          "additionalProperties": false,
          "description": "Analytics metadata describing the tool call's purpose",
          "properties": {
            "context": {
              "description": "Explain why calling this tool fits the user's overall goal. This parameter supports analytics and user-intent tracking. Provide 15-25 meaningful words in third-person perspective. Avoid credentials, passwords, and personal data; the server does not classify sensitive content.",
              "maxLength": 256,
              "type": "string"
            }
          },
          "required": [
            "context"
          ],
          "type": "object"
        }
      },
      "required": [
        "telemetry",
        "org_slug",
        "name"
      ],
      "type": "object"
    }
  },
  {
    "name": "create_cluster_queue",
    "description": "Create a new queue in a cluster",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "cluster_id": {
          "type": "string"
        },
        "description": {
          "description": "Description of the queue",
          "type": "string"
        },
        "key": {
          "type": "string"
        },
        "org_slug": {
          "type": "string"
        },
        "telemetry": {
          "additionalProperties": false,
          "description": "Analytics metadata describing the tool call's purpose",
          "properties": {
            "context": {
              "description": "Explain why calling this tool fits the user's overall goal. This parameter supports analytics and user-intent tracking. Provide 15-25 meaningful words in third-person perspective. Avoid credentials, passwords, and personal data; the server does not classify sensitive content.",
              "maxLength": 256,
              "type": "string"
            }
          },
          "required": [
            "context"
          ],
          "type": "object"
        }
      },
      "required": [
        "telemetry",
        "org_slug",
        "cluster_id",
        "key"
      ],
      "type": "object"
    }
  },
  {
    "name": "create_cluster_secret",
    "description": "Create cluster secret",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "cluster_id": {
          "type": "string"
        },
        "description": {
          "description": "Optional description of the secret",
          "type": "string"
        },
        "key": {
          "description": "Secret key; must start with a letter, contain only letters, numbers, and underscores, be at most 255 characters, and not start with buildkite or bk (case-insensitive)",
          "type": "string"
        },
        "org_slug": {
          "type": "string"
        },
        "policy": {
          "description": "Access policy as a YAML list of rules; omitting it allows access from all agents in the cluster",
          "type": "string"
        },
        "telemetry": {
          "additionalProperties": false,
          "description": "Analytics metadata describing the tool call's purpose",
          "properties": {
            "context": {
              "description": "Explain why calling this tool fits the user's overall goal. This parameter supports analytics and user-intent tracking. Provide 15-25 meaningful words in third-person perspective. Avoid credentials, passwords, and personal data; the server does not classify sensitive content.",
              "maxLength": 256,
              "type": "string"
            }
          },
          "required": [
            "context"
          ],
          "type": "object"
        },
        "value": {
          "description": "Required non-blank write-only secret value under 8 KiB; it is never returned by the API",
          "type": "string"
        }
      },
      "required": [
        "telemetry",
        "org_slug",
        "cluster_id",
        "key",
        "value"
      ],
      "type": "object"
    }
  },
  {
    "name": "create_pipeline",
    "description": "Set up a new CI/CD pipeline in Buildkite with YAML configuration, repository connection, and cluster assignment",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "cancel_running_branch_builds": {
          "description": "Cancel running builds when new builds are created on the same branch",
          "type": "boolean"
        },
        "cluster_id": {
          "description": "The cluster ID to assign the pipeline to",
          "type": "string"
        },
        "configuration": {
          "description": "The pipeline configuration in YAML format",
          "type": "string"
        },
        "create_webhook": {
          "description": "Create a GitHub webhook to trigger builds on pull-request and push events",
          "type": "boolean"
        },
        "default_branch": {
          "description": "The default branch for builds and metrics filtering",
          "type": "string"
        },
        "description": {
          "type": "string"
        },
        "name": {
          "type": "string"
        },
        "org_slug": {
          "type": "string"
        },
        "repository_url": {
          "description": "The Git repository URL",
          "type": "string"
        },
        "skip_queued_branch_builds": {
          "description": "Skip intermediate builds when new builds are created on the same branch",
          "type": "boolean"
        },
        "tags": {
          "description": "Tags to apply to the pipeline for filtering and organization",
          "items": {
            "type": "string"
          },
          "type": [
            "null",
            "array"
          ]
        },
        "teams": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "Team UUIDs mapped to their access level on the pipeline: read_only, build_and_read or manage_build_and_read. Organizations with teams enabled require at least one team, unless the user is an organization administrator",
          "type": "object"
        },
        "telemetry": {
          "additionalProperties": false,
          "description": "Analytics metadata describing the tool call's purpose",
          "properties": {
            "context": {
              "description": "Explain why calling this tool fits the user's overall goal. This parameter supports analytics and user-intent tracking. Provide 15-25 meaningful words in third-person perspective. Avoid credentials, passwords, and personal data; the server does not classify sensitive content.",
              "maxLength": 256,
              "type": "string"
            }
          },
          "required": [
            "context"
          ],
          "type": "object"
        }
      },
      "required": [
        "telemetry",
        "org_slug",
        "name",
        "repository_url",
        "cluster_id",
        "configuration"
      ],
      "type": "object"
    }
  },
  {
    "name": "create_pipeline_schedule",
    "description": "Create a new pipeline schedule that triggers builds on a cron-driven interval",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "branch": {
          "description": "Target branch (defaults to the pipeline default branch)",
          "type": "string"
        },
        "commit": {
          "description": "Commit reference (defaults to HEAD)",
          "type": "string"
        },
        "cronline": {
          "description": "Schedule interval as a crontab expression (e.g. '0 0 * * *') or predefined value (e.g. '@daily', '@hourly', '@weekly', '@monthly', '@yearly')",
          "type": "string"
        },
        "enabled": {
          "description": "Whether the schedule is active. Defaults to true if unset.",
          "type": [
            "null",
            "boolean"
          ]
        },
        "env": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "Environment variables to set on triggered builds",
          "type": "object"
        },
        "label": {
          "description": "Descriptive label for the schedule",
          "type": "string"
        },
        "message": {
          "description": "Message attached to triggered builds",
          "type": "string"
        },
        "org_slug": {
          "type": "string"
        },
        "pipeline_slug": {
          "type": "string"
        },
        "telemetry": {
          "additionalProperties": false,
          "description": "Analytics metadata describing the tool call's purpose",
          "properties": {
            "context": {
              "description": "Explain why calling this tool fits the user's overall goal. This parameter supports analytics and user-intent tracking. Provide 15-25 meaningful words in third-person perspective. Avoid credentials, passwords, and personal data; the server does not classify sensitive content.",
              "maxLength": 256,
              "type": "string"
            }
          },
          "required": [
            "context"
          ],
          "type": "object"
        }
      },
      "required": [
        "telemetry",
        "org_slug",
        "pipeline_slug",
        "cronline"
      ],
      "type": "object"
    }
  },
  {
    "name": "current_user",
    "description": "Get details about the user account that owns the API token, including name, email, avatar, and account creation date",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "telemetry": {
          "additionalProperties": false,
          "description": "Analytics metadata describing the tool call's purpose",
          "properties": {
            "context": {
              "description": "Explain why calling this tool fits the user's overall goal. This parameter supports analytics and user-intent tracking. Provide 15-25 meaningful words in third-person perspective. Avoid credentials, passwords, and personal data; the server does not classify sensitive content.",
              "maxLength": 256,
              "type": "string"
            }
          },
          "required": [
            "context"
          ],
          "type": "object"
        }
      },
      "required": [
        "telemetry"
      ],
      "type": "object"
    }
  },
  {
    "name": "get_agent",
    "description": "Get detailed information about a specific agent including its connection state, host details, current job, metadata, and pause status",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "agent_id": {
          "type": "string"
        },
        "detail_level": {
          "description": "Response detail level: 'summary', 'detailed', or 'full' (default)",
          "type": "string"
        },
        "org_slug": {
          "type": "string"
        },
        "telemetry": {
          "additionalProperties": false,
          "description": "Analytics metadata describing the tool call's purpose",
          "properties": {
            "context": {
              "description": "Explain why calling this tool fits the user's overall goal. This parameter supports analytics and user-intent tracking. Provide 15-25 meaningful words in third-person perspective. Avoid credentials, passwords, and personal data; the server does not classify sensitive content.",
              "maxLength": 256,
              "type": "string"
            }
          },
          "required": [
            "context"
          ],
          "type": "object"
        }
      },
      "required": [
        "telemetry",
        "org_slug",
        "agent_id"
      ],
      "type": "object"
    }
  },
  {
    "name": "get_artifact",
    "description": "Get a specific artifact by organization, pipeline, build, job, and artifact identifiers. Text artifacts under 64 KiB are returned inline in `content`; larger or binary artifacts return metadata plus a short-lived `download_url`. When `download_url_auth` is \"none\" the URL is presigned (a buildkiteartifacts.com S3 link) — fetch it with a plain GET and NO Authorization header. It expires after `download_url_expires_in_seconds`; if it has expired, call this tool again for a fresh URL.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "artifact_id": {
          "description": "The UUID of the artifact to download",
          "type": "string"
        },
        "build_number": {
          "type": "string"
        },
        "job_id": {
          "description": "The UUID of the job that produced the artifact",
          "type": "string"
        },
        "org_slug": {
          "type": "string"
        },
        "pipeline_slug": {
          "type": "string"
        },
        "telemetry": {
          "additionalProperties": false,
          "description": "Analytics metadata describing the tool call's purpose",
          "properties": {
            "context": {
              "description": "Explain why calling this tool fits the user's overall goal. This parameter supports analytics and user-intent tracking. Provide 15-25 meaningful words in third-person perspective. Avoid credentials, passwords, and personal data; the server does not classify sensitive content.",
              "maxLength": 256,
              "type": "string"
            }
          },
          "required": [
            "context"
          ],
          "type": "object"
        }
      },
      "required": [
        "telemetry",
        "org_slug",
        "pipeline_slug",
        "build_number",
        "job_id",
        "artifact_id"
      ],
      "type": "object"
    }
  },
  {
    "name": "get_build",
    "description": "Get a single build with lightweight annotation summaries. Annotation bodies and jobs are not included — use list_annotations to read annotations, list_jobs or get_job for job detail, and list_step_uploads to see the dynamic pipeline configuration the build received via `buildkite-agent pipeline upload`",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "build_number": {
          "type": "string"
        },
        "org_slug": {
          "type": "string"
        },
        "pipeline_slug": {
          "type": "string"
        },
        "telemetry": {
          "additionalProperties": false,
          "description": "Analytics metadata describing the tool call's purpose",
          "properties": {
            "context": {
              "description": "Explain why calling this tool fits the user's overall goal. This parameter supports analytics and user-intent tracking. Provide 15-25 meaningful words in third-person perspective. Avoid credentials, passwords, and personal data; the server does not classify sensitive content.",
              "maxLength": 256,
              "type": "string"
            }
          },
          "required": [
            "context"
          ],
          "type": "object"
        }
      },
      "required": [
        "telemetry",
        "org_slug",
        "pipeline_slug",
        "build_number"
      ],
      "type": "object"
    }
  },
  {
    "name": "get_build_failure_summary",
    "description": "Diagnose a Buildkite build failure in one call. Returns build.state, build.job_state_counts tallying every job in the build by state (when present, use it to confirm the returned problem jobs are the build's only problems without calling list_jobs), terminal problem jobs, downstream failed or broken jobs, promised failures from running jobs, and size-bounded diagnostic content from logs, annotations, and failed Test Engine executions. Annotation content is in the body_html field; there is no body field. Start with this tool before calling individual job, log, annotation, or test tools.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "build_number": {
          "type": "string"
        },
        "content_limit_bytes": {
          "description": "Maximum bytes for the response payload (default and max 262144); lower it to fit clients with small tool-result limits, combining with log_tail and max_jobs for finer trimming",
          "type": "integer"
        },
        "include_annotations": {
          "description": "Include error and warning annotation bodies (default true)",
          "type": [
            "null",
            "boolean"
          ]
        },
        "include_failed_tests": {
          "description": "Include failed Test Engine executions when the build has Test Engine runs (default true)",
          "type": [
            "null",
            "boolean"
          ]
        },
        "include_failure_expanded": {
          "description": "Include expanded test failure details such as stack traces within the summary's bounded test-content budget",
          "type": "boolean"
        },
        "include_logs": {
          "description": "Include a bounded log tail for failed, timed-out, canceled, and promised-failing jobs (default true)",
          "type": [
            "null",
            "boolean"
          ]
        },
        "log_tail": {
          "description": "Log lines to include for each failed, timed-out, canceled, or promised-failing job (default 50, max 200)",
          "type": "integer"
        },
        "max_annotations": {
          "description": "Maximum error or warning annotations to return (default 20, max 100); the server scans at most 500 total annotations",
          "type": "integer"
        },
        "max_failed_tests": {
          "description": "Maximum failed test executions to return across all Test Engine runs (default 100, max 200)",
          "type": "integer"
        },
        "max_failed_tests_per_run": {
          "description": "Maximum failed test executions to return per Test Engine run (default 20, max 100)",
          "type": "integer"
        },
        "max_jobs": {
          "description": "Maximum terminal problem or downstream-failed jobs to return (default 10, server may enforce a lower maximum, absolute max 50)",
          "type": "integer"
        },
        "max_test_runs": {
          "description": "Maximum Test Engine runs to inspect (default 5, max 20)",
          "type": "integer"
        },
        "org_slug": {
          "type": "string"
        },
        "pipeline_slug": {
          "type": "string"
        },
        "telemetry": {
          "additionalProperties": false,
          "description": "Analytics metadata describing the tool call's purpose",
          "properties": {
            "context": {
              "description": "Explain why calling this tool fits the user's overall goal. This parameter supports analytics and user-intent tracking. Provide 15-25 meaningful words in third-person perspective. Avoid credentials, passwords, and personal data; the server does not classify sensitive content.",
              "maxLength": 256,
              "type": "string"
            }
          },
          "required": [
            "context"
          ],
          "type": "object"
        }
      },
      "required": [
        "telemetry",
        "org_slug",
        "pipeline_slug",
        "build_number"
      ],
      "type": "object"
    }
  },
  {
    "name": "get_build_test_engine_runs",
    "description": "Get test engine runs data for a specific build in Buildkite. This can be used to look up Test Runs.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "build_number": {
          "type": "string"
        },
        "org_slug": {
          "type": "string"
        },
        "pipeline_slug": {
          "type": "string"
        },
        "telemetry": {
          "additionalProperties": false,
          "description": "Analytics metadata describing the tool call's purpose",
          "properties": {
            "context": {
              "description": "Explain why calling this tool fits the user's overall goal. This parameter supports analytics and user-intent tracking. Provide 15-25 meaningful words in third-person perspective. Avoid credentials, passwords, and personal data; the server does not classify sensitive content.",
              "maxLength": 256,
              "type": "string"
            }
          },
          "required": [
            "context"
          ],
          "type": "object"
        }
      },
      "required": [
        "telemetry",
        "org_slug",
        "pipeline_slug",
        "build_number"
      ],
      "type": "object"
    }
  },
  {
    "name": "get_cluster",
    "description": "Get detailed information about a specific cluster including its name, description, default queue, and configuration",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "cluster_id": {
          "type": "string"
        },
        "org_slug": {
          "type": "string"
        },
        "telemetry": {
          "additionalProperties": false,
          "description": "Analytics metadata describing the tool call's purpose",
          "properties": {
            "context": {
              "description": "Explain why calling this tool fits the user's overall goal. This parameter supports analytics and user-intent tracking. Provide 15-25 meaningful words in third-person perspective. Avoid credentials, passwords, and personal data; the server does not classify sensitive content.",
              "maxLength": 256,
              "type": "string"
            }
          },
          "required": [
            "context"
          ],
          "type": "object"
        }
      },
      "required": [
        "telemetry",
        "org_slug",
        "cluster_id"
      ],
      "type": "object"
    }
  },
  {
    "name": "get_cluster_queue",
    "description": "Get detailed information about a specific queue including its key, description, dispatch status, and hosted agent configuration",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "cluster_id": {
          "type": "string"
        },
        "org_slug": {
          "type": "string"
        },
        "queue_id": {
          "type": "string"
        },
        "telemetry": {
          "additionalProperties": false,
          "description": "Analytics metadata describing the tool call's purpose",
          "properties": {
            "context": {
              "description": "Explain why calling this tool fits the user's overall goal. This parameter supports analytics and user-intent tracking. Provide 15-25 meaningful words in third-person perspective. Avoid credentials, passwords, and personal data; the server does not classify sensitive content.",
              "maxLength": 256,
              "type": "string"
            }
          },
          "required": [
            "context"
          ],
          "type": "object"
        }
      },
      "required": [
        "telemetry",
        "org_slug",
        "cluster_id",
        "queue_id"
      ],
      "type": "object"
    }
  },
  {
    "name": "get_cluster_secret",
    "description": "Get cluster secret information",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "cluster_id": {
          "type": "string"
        },
        "org_slug": {
          "type": "string"
        },
        "secret_id": {
          "type": "string"
        },
        "telemetry": {
          "additionalProperties": false,
          "description": "Analytics metadata describing the tool call's purpose",
          "properties": {
            "context": {
              "description": "Explain why calling this tool fits the user's overall goal. This parameter supports analytics and user-intent tracking. Provide 15-25 meaningful words in third-person perspective. Avoid credentials, passwords, and personal data; the server does not classify sensitive content.",
              "maxLength": 256,
              "type": "string"
            }
          },
          "required": [
            "context"
          ],
          "type": "object"
        }
      },
      "required": [
        "telemetry",
        "org_slug",
        "cluster_id",
        "secret_id"
      ],
      "type": "object"
    }
  },
  {
    "name": "get_failed_executions",
    "description": "Get failed test executions for a specific test run in Buildkite Test Engine. Optionally get the expanded failure details such as full error messages and stack traces.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "include_failure_expanded": {
          "description": "Include expanded failure details such as full error messages and stack traces",
          "type": "boolean"
        },
        "org_slug": {
          "type": "string"
        },
        "page": {
          "description": "Page number for pagination (min 1)",
          "type": "integer"
        },
        "per_page": {
          "description": "Results per page for pagination (min 1, max 100)",
          "type": "integer"
        },
        "run_id": {
          "type": "string"
        },
        "telemetry": {
          "additionalProperties": false,
          "description": "Analytics metadata describing the tool call's purpose",
          "properties": {
            "context": {
              "description": "Explain why calling this tool fits the user's overall goal. This parameter supports analytics and user-intent tracking. Provide 15-25 meaningful words in third-person perspective. Avoid credentials, passwords, and personal data; the server does not classify sensitive content.",
              "maxLength": 256,
              "type": "string"
            }
          },
          "required": [
            "context"
          ],
          "type": "object"
        },
        "test_suite_slug": {
          "type": "string"
        }
      },
      "required": [
        "telemetry",
        "org_slug",
        "test_suite_slug",
        "run_id"
      ],
      "type": "object"
    }
  },
  {
    "name": "get_job",
    "description": "Get a single job by its UUID. Provide 'pipeline_slug' and 'build_number' for a build-scoped lookup, or omit both to look the job up by organization and job ID alone",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "build_number": {
          "description": "Build number. Provide together with 'pipeline_slug' for a build-scoped lookup. Omit both to look up the job by organization and job ID alone",
          "type": "string"
        },
        "include_agent": {
          "description": "Include full agent details in job objects. When false (default), only agent.id is included",
          "type": "boolean"
        },
        "job_id": {
          "type": "string"
        },
        "org_slug": {
          "type": "string"
        },
        "pipeline_slug": {
          "description": "Pipeline slug. Provide together with 'build_number' for a build-scoped lookup. Omit both to look up the job by organization and job ID alone",
          "type": "string"
        },
        "telemetry": {
          "additionalProperties": false,
          "description": "Analytics metadata describing the tool call's purpose",
          "properties": {
            "context": {
              "description": "Explain why calling this tool fits the user's overall goal. This parameter supports analytics and user-intent tracking. Provide 15-25 meaningful words in third-person perspective. Avoid credentials, passwords, and personal data; the server does not classify sensitive content.",
              "maxLength": 256,
              "type": "string"
            }
          },
          "required": [
            "context"
          ],
          "type": "object"
        }
      },
      "required": [
        "telemetry",
        "org_slug",
        "job_id"
      ],
      "type": "object"
    }
  },
  {
    "name": "get_job_env",
    "description": "Get the environment variables for a specific job in a Buildkite build",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "build_number": {
          "type": "string"
        },
        "job_id": {
          "type": "string"
        },
        "org_slug": {
          "type": "string"
        },
        "pipeline_slug": {
          "type": "string"
        },
        "telemetry": {
          "additionalProperties": false,
          "description": "Analytics metadata describing the tool call's purpose",
          "properties": {
            "context": {
              "description": "Explain why calling this tool fits the user's overall goal. This parameter supports analytics and user-intent tracking. Provide 15-25 meaningful words in third-person perspective. Avoid credentials, passwords, and personal data; the server does not classify sensitive content.",
              "maxLength": 256,
              "type": "string"
            }
          },
          "required": [
            "context"
          ],
          "type": "object"
        }
      },
      "required": [
        "telemetry",
        "org_slug",
        "pipeline_slug",
        "build_number",
        "job_id"
      ],
      "type": "object"
    }
  },
  {
    "name": "get_pipeline",
    "description": "Get detailed information about a specific pipeline including its configuration, steps, environment variables, and build statistics",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "detail_level": {
          "description": "Response detail level: 'summary', 'detailed', or 'full' (default)",
          "type": "string"
        },
        "org_slug": {
          "type": "string"
        },
        "pipeline_slug": {
          "type": "string"
        },
        "telemetry": {
          "additionalProperties": false,
          "description": "Analytics metadata describing the tool call's purpose",
          "properties": {
            "context": {
              "description": "Explain why calling this tool fits the user's overall goal. This parameter supports analytics and user-intent tracking. Provide 15-25 meaningful words in third-person perspective. Avoid credentials, passwords, and personal data; the server does not classify sensitive content.",
              "maxLength": 256,
              "type": "string"
            }
          },
          "required": [
            "context"
          ],
          "type": "object"
        }
      },
      "required": [
        "telemetry",
        "org_slug",
        "pipeline_slug"
      ],
      "type": "object"
    }
  },
  {
    "name": "get_pipeline_schedule",
    "description": "Get detailed information about a single pipeline schedule including its cron expression, target branch, environment variables, enabled state, last failure, and next build time",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "org_slug": {
          "type": "string"
        },
        "pipeline_slug": {
          "type": "string"
        },
        "schedule_id": {
          "type": "string"
        },
        "telemetry": {
          "additionalProperties": false,
          "description": "Analytics metadata describing the tool call's purpose",
          "properties": {
            "context": {
              "description": "Explain why calling this tool fits the user's overall goal. This parameter supports analytics and user-intent tracking. Provide 15-25 meaningful words in third-person perspective. Avoid credentials, passwords, and personal data; the server does not classify sensitive content.",
              "maxLength": 256,
              "type": "string"
            }
          },
          "required": [
            "context"
          ],
          "type": "object"
        }
      },
      "required": [
        "telemetry",
        "org_slug",
        "pipeline_slug",
        "schedule_id"
      ],
      "type": "object"
    }
  },
  {
    "name": "get_step_upload",
    "description": "Get a single dynamic pipeline upload including its pipeline definition rendered as YAML (definition_yaml). Definitions over the API's render limit are omitted: definition_yaml is omitted, definition_yaml_omitted is true and definition_bytes reports the size. Only available while the build is within its maximum lifetime (~30 days); older builds return 410 Gone",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "build_number": {
          "type": "string"
        },
        "org_slug": {
          "type": "string"
        },
        "pipeline_slug": {
          "type": "string"
        },
        "telemetry": {
          "additionalProperties": false,
          "description": "Analytics metadata describing the tool call's purpose",
          "properties": {
            "context": {
              "description": "Explain why calling this tool fits the user's overall goal. This parameter supports analytics and user-intent tracking. Provide 15-25 meaningful words in third-person perspective. Avoid credentials, passwords, and personal data; the server does not classify sensitive content.",
              "maxLength": 256,
              "type": "string"
            }
          },
          "required": [
            "context"
          ],
          "type": "object"
        },
        "upload_uuid": {
          "description": "UUID of the step upload, from list_step_uploads",
          "type": "string"
        }
      },
      "required": [
        "telemetry",
        "org_slug",
        "pipeline_slug",
        "build_number",
        "upload_uuid"
      ],
      "type": "object"
    }
  },
  {
    "name": "get_test",
    "description": "Get a specific test in Buildkite Test Engine, including execution metrics aggregated over a selected time window. This provides additional metadata for failed test executions.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "max_timestamp": {
          "description": "End of the aggregation window in RFC3339 format. Defaults to the current time when omitted.",
          "type": "string"
        },
        "min_timestamp": {
          "description": "Start of the aggregation window in RFC3339 format. When omitted, defaults to the organization's default Test Engine period before the current time.",
          "type": "string"
        },
        "org_slug": {
          "type": "string"
        },
        "period": {
          "description": "Relative aggregation window for the returned metrics, such as '7days' or '28days'. Whether selected by period or timestamps, the aggregation window cannot exceed the organization's maximum Test Engine window. Cannot be combined with min_timestamp or max_timestamp.",
          "type": "string"
        },
        "telemetry": {
          "additionalProperties": false,
          "description": "Analytics metadata describing the tool call's purpose",
          "properties": {
            "context": {
              "description": "Explain why calling this tool fits the user's overall goal. This parameter supports analytics and user-intent tracking. Provide 15-25 meaningful words in third-person perspective. Avoid credentials, passwords, and personal data; the server does not classify sensitive content.",
              "maxLength": 256,
              "type": "string"
            }
          },
          "required": [
            "context"
          ],
          "type": "object"
        },
        "test_id": {
          "type": "string"
        },
        "test_suite_slug": {
          "type": "string"
        }
      },
      "required": [
        "telemetry",
        "org_slug",
        "test_suite_slug",
        "test_id"
      ],
      "type": "object"
    }
  },
  {
    "name": "get_test_run",
    "description": "Get a specific test run in Buildkite Test Engine",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "org_slug": {
          "type": "string"
        },
        "run_id": {
          "type": "string"
        },
        "telemetry": {
          "additionalProperties": false,
          "description": "Analytics metadata describing the tool call's purpose",
          "properties": {
            "context": {
              "description": "Explain why calling this tool fits the user's overall goal. This parameter supports analytics and user-intent tracking. Provide 15-25 meaningful words in third-person perspective. Avoid credentials, passwords, and personal data; the server does not classify sensitive content.",
              "maxLength": 256,
              "type": "string"
            }
          },
          "required": [
            "context"
          ],
          "type": "object"
        },
        "test_suite_slug": {
          "type": "string"
        }
      },
      "required": [
        "telemetry",
        "org_slug",
        "test_suite_slug",
        "run_id"
      ],
      "type": "object"
    }
  },
  {
    "name": "list_agents",
    "description": "List agents in an organization with their connection state, host details, version, current job, and pause status",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "cluster_queue_id": {
          "description": "Filter agents by cluster queue ID (UUID)",
          "type": "string"
        },
        "detail_level": {
          "description": "Response detail level: 'summary' (default), 'detailed', or 'full'",
          "type": "string"
        },
        "hostname": {
          "type": "string"
        },
        "name": {
          "type": "string"
        },
        "org_slug": {
          "type": "string"
        },
        "page": {
          "description": "Page number for pagination (min 1)",
          "type": "integer"
        },
        "per_page": {
          "description": "Results per page for pagination (min 1, max 100)",
          "type": "integer"
        },
        "telemetry": {
          "additionalProperties": false,
          "description": "Analytics metadata describing the tool call's purpose",
          "properties": {
            "context": {
              "description": "Explain why calling this tool fits the user's overall goal. This parameter supports analytics and user-intent tracking. Provide 15-25 meaningful words in third-person perspective. Avoid credentials, passwords, and personal data; the server does not classify sensitive content.",
              "maxLength": 256,
              "type": "string"
            }
          },
          "required": [
            "context"
          ],
          "type": "object"
        },
        "version": {
          "type": "string"
        }
      },
      "required": [
        "telemetry",
        "org_slug"
      ],
      "type": "object"
    }
  },
  {
    "name": "list_annotations",
    "description": "List annotations for a build or a specific job. Use scope='build' (default) or scope='job' with job_id. Annotation content is in the body_html field; there is no body field",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "build_number": {
          "type": "string"
        },
        "job_id": {
          "description": "Job ID required when scope is job",
          "type": "string"
        },
        "org_slug": {
          "type": "string"
        },
        "page": {
          "description": "Page number for pagination (min 1)",
          "type": "integer"
        },
        "per_page": {
          "description": "Results per page for pagination (min 1, max 100)",
          "type": "integer"
        },
        "pipeline_slug": {
          "type": "string"
        },
        "scope": {
          "description": "Annotation scope: 'build' (default) or 'job'. When 'job', job_id is required.",
          "type": "string"
        },
        "telemetry": {
          "additionalProperties": false,
          "description": "Analytics metadata describing the tool call's purpose",
          "properties": {
            "context": {
              "description": "Explain why calling this tool fits the user's overall goal. This parameter supports analytics and user-intent tracking. Provide 15-25 meaningful words in third-person perspective. Avoid credentials, passwords, and personal data; the server does not classify sensitive content.",
              "maxLength": 256,
              "type": "string"
            }
          },
          "required": [
            "context"
          ],
          "type": "object"
        }
      },
      "required": [
        "telemetry",
        "org_slug",
        "pipeline_slug",
        "build_number"
      ],
      "type": "object"
    }
  },
  {
    "name": "list_artifacts_for_build",
    "description": "List all artifacts for a build across all jobs, including filenames, paths, sizes, and MIME types. Output can be large for big builds — use per_page and paginate. To fetch an artifact's contents or a download URL, call get_artifact with that artifact's id and job_id.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "build_number": {
          "type": "string"
        },
        "org_slug": {
          "type": "string"
        },
        "page": {
          "description": "Page number for pagination (min 1)",
          "type": "integer"
        },
        "per_page": {
          "description": "Results per page for pagination (min 1, max 100)",
          "type": "integer"
        },
        "pipeline_slug": {
          "type": "string"
        },
        "telemetry": {
          "additionalProperties": false,
          "description": "Analytics metadata describing the tool call's purpose",
          "properties": {
            "context": {
              "description": "Explain why calling this tool fits the user's overall goal. This parameter supports analytics and user-intent tracking. Provide 15-25 meaningful words in third-person perspective. Avoid credentials, passwords, and personal data; the server does not classify sensitive content.",
              "maxLength": 256,
              "type": "string"
            }
          },
          "required": [
            "context"
          ],
          "type": "object"
        }
      },
      "required": [
        "telemetry",
        "org_slug",
        "pipeline_slug",
        "build_number"
      ],
      "type": "object"
    }
  },
  {
    "name": "list_artifacts_for_job",
    "description": "List all artifacts for an individual job, including filenames, paths, sizes, and MIME types. To fetch an artifact's contents or a download URL, call get_artifact with that artifact's id and job_id.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "build_number": {
          "type": "string"
        },
        "job_id": {
          "type": "string"
        },
        "org_slug": {
          "type": "string"
        },
        "page": {
          "description": "Page number for pagination (min 1)",
          "type": "integer"
        },
        "per_page": {
          "description": "Results per page for pagination (min 1, max 100)",
          "type": "integer"
        },
        "pipeline_slug": {
          "type": "string"
        },
        "telemetry": {
          "additionalProperties": false,
          "description": "Analytics metadata describing the tool call's purpose",
          "properties": {
            "context": {
              "description": "Explain why calling this tool fits the user's overall goal. This parameter supports analytics and user-intent tracking. Provide 15-25 meaningful words in third-person perspective. Avoid credentials, passwords, and personal data; the server does not classify sensitive content.",
              "maxLength": 256,
              "type": "string"
            }
          },
          "required": [
            "context"
          ],
          "type": "object"
        }
      },
      "required": [
        "telemetry",
        "org_slug",
        "pipeline_slug",
        "build_number",
        "job_id"
      ],
      "type": "object"
    }
  },
  {
    "name": "list_builds",
    "description": "List builds for a pipeline or across all pipelines in an organization, returning a lightweight summary of each build. When pipeline_slug is omitted, lists builds across all pipelines in the organization. Jobs are not included — use list_jobs or get_job for job detail",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "branch": {
          "description": "Filter builds by git branch name",
          "type": "string"
        },
        "commit": {
          "description": "Filter builds by specific commit SHA",
          "type": "string"
        },
        "creator": {
          "description": "Filter builds by build creator",
          "type": "string"
        },
        "org_slug": {
          "type": "string"
        },
        "page": {
          "description": "Page number for pagination (min 1)",
          "type": "integer"
        },
        "per_page": {
          "description": "Results per page for pagination (min 1, max 100)",
          "type": "integer"
        },
        "pipeline_slug": {
          "description": "Filter builds by pipeline. When omitted, lists builds across all pipelines in the organization",
          "type": "string"
        },
        "state": {
          "description": "Filter builds by state (scheduled, running, passed, failed, canceled, skipped)",
          "type": "string"
        },
        "telemetry": {
          "additionalProperties": false,
          "description": "Analytics metadata describing the tool call's purpose",
          "properties": {
            "context": {
              "description": "Explain why calling this tool fits the user's overall goal. This parameter supports analytics and user-intent tracking. Provide 15-25 meaningful words in third-person perspective. Avoid credentials, passwords, and personal data; the server does not classify sensitive content.",
              "maxLength": 256,
              "type": "string"
            }
          },
          "required": [
            "context"
          ],
          "type": "object"
        }
      },
      "required": [
        "telemetry",
        "org_slug"
      ],
      "type": "object"
    }
  },
  {
    "name": "list_cluster_queues",
    "description": "List all queues in a cluster with their keys, descriptions, dispatch status, and agent configuration",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "cluster_id": {
          "type": "string"
        },
        "org_slug": {
          "type": "string"
        },
        "page": {
          "description": "Page number for pagination (min 1)",
          "type": "integer"
        },
        "per_page": {
          "description": "Results per page for pagination (min 1, max 100)",
          "type": "integer"
        },
        "telemetry": {
          "additionalProperties": false,
          "description": "Analytics metadata describing the tool call's purpose",
          "properties": {
            "context": {
              "description": "Explain why calling this tool fits the user's overall goal. This parameter supports analytics and user-intent tracking. Provide 15-25 meaningful words in third-person perspective. Avoid credentials, passwords, and personal data; the server does not classify sensitive content.",
              "maxLength": 256,
              "type": "string"
            }
          },
          "required": [
            "context"
          ],
          "type": "object"
        }
      },
      "required": [
        "telemetry",
        "org_slug",
        "cluster_id"
      ],
      "type": "object"
    }
  },
  {
    "name": "list_cluster_secrets",
    "description": "List cluster secrets",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "cluster_id": {
          "type": "string"
        },
        "org_slug": {
          "type": "string"
        },
        "page": {
          "description": "Page number for pagination (min 1)",
          "type": "integer"
        },
        "per_page": {
          "description": "Results per page for pagination (min 1, max 100)",
          "type": "integer"
        },
        "telemetry": {
          "additionalProperties": false,
          "description": "Analytics metadata describing the tool call's purpose",
          "properties": {
            "context": {
              "description": "Explain why calling this tool fits the user's overall goal. This parameter supports analytics and user-intent tracking. Provide 15-25 meaningful words in third-person perspective. Avoid credentials, passwords, and personal data; the server does not classify sensitive content.",
              "maxLength": 256,
              "type": "string"
            }
          },
          "required": [
            "context"
          ],
          "type": "object"
        }
      },
      "required": [
        "telemetry",
        "org_slug",
        "cluster_id"
      ],
      "type": "object"
    }
  },
  {
    "name": "list_clusters",
    "description": "List all clusters in an organization with their names, descriptions, default queues, and creation details",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "org_slug": {
          "type": "string"
        },
        "page": {
          "description": "Page number for pagination (min 1)",
          "type": "integer"
        },
        "per_page": {
          "description": "Results per page for pagination (min 1, max 100)",
          "type": "integer"
        },
        "telemetry": {
          "additionalProperties": false,
          "description": "Analytics metadata describing the tool call's purpose",
          "properties": {
            "context": {
              "description": "Explain why calling this tool fits the user's overall goal. This parameter supports analytics and user-intent tracking. Provide 15-25 meaningful words in third-person perspective. Avoid credentials, passwords, and personal data; the server does not classify sensitive content.",
              "maxLength": 256,
              "type": "string"
            }
          },
          "required": [
            "context"
          ],
          "type": "object"
        }
      },
      "required": [
        "telemetry",
        "org_slug"
      ],
      "type": "object"
    }
  },
  {
    "name": "list_jobs",
    "description": "List jobs for a Buildkite build, returning an actionable summary by default. For CI failure diagnosis, use state='failed,broken' to avoid returning successful jobs. Use detail_level='detailed' for execution metadata or 'full' for the existing full MCP job response. Returns 'items' and cursor pagination 'links'",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "after": {
          "description": "Cursor for the next page. Take this from the 'links.next' URL of a previous response. Mutually exclusive with 'before'",
          "type": "string"
        },
        "before": {
          "description": "Cursor for the previous page. Take this from a previous response. Mutually exclusive with 'after'",
          "type": "string"
        },
        "build_number": {
          "type": "string"
        },
        "detail_level": {
          "description": "Response detail level: 'summary' (default), 'detailed', or 'full'",
          "type": "string"
        },
        "group_key": {
          "description": "Filter jobs by group key. Includes all jobs in the group",
          "type": "string"
        },
        "include_agent": {
          "description": "Include full agent details at the detailed and full levels. When false (default), detailed and full responses include only agent.id",
          "type": "boolean"
        },
        "include_retried_jobs": {
          "description": "Include retried jobs in the response. Defaults to true on the server when omitted",
          "type": [
            "null",
            "boolean"
          ]
        },
        "org_slug": {
          "type": "string"
        },
        "per_page": {
          "description": "Results per page for cursor pagination (min 1, max 100, default 30)",
          "type": "integer"
        },
        "pipeline_slug": {
          "type": "string"
        },
        "state": {
          "description": "Filter jobs by state. Comma-separated for multiple states (e.g., 'passed,failed,running')",
          "type": "string"
        },
        "step_key": {
          "description": "Filter jobs by step key. Includes all parallel jobs for the step",
          "type": "string"
        },
        "telemetry": {
          "additionalProperties": false,
          "description": "Analytics metadata describing the tool call's purpose",
          "properties": {
            "context": {
              "description": "Explain why calling this tool fits the user's overall goal. This parameter supports analytics and user-intent tracking. Provide 15-25 meaningful words in third-person perspective. Avoid credentials, passwords, and personal data; the server does not classify sensitive content.",
              "maxLength": 256,
              "type": "string"
            }
          },
          "required": [
            "context"
          ],
          "type": "object"
        }
      },
      "required": [
        "telemetry",
        "org_slug",
        "pipeline_slug",
        "build_number"
      ],
      "type": "object"
    }
  },
  {
    "name": "list_pipeline_schedules",
    "description": "List the pipeline schedules for a pipeline, including cron expression, target branch, environment variables, enabled state, and next scheduled build time",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "org_slug": {
          "type": "string"
        },
        "page": {
          "description": "Page number for pagination (min 1)",
          "type": "integer"
        },
        "per_page": {
          "description": "Results per page for pagination (min 1, max 100)",
          "type": "integer"
        },
        "pipeline_slug": {
          "type": "string"
        },
        "telemetry": {
          "additionalProperties": false,
          "description": "Analytics metadata describing the tool call's purpose",
          "properties": {
            "context": {
              "description": "Explain why calling this tool fits the user's overall goal. This parameter supports analytics and user-intent tracking. Provide 15-25 meaningful words in third-person perspective. Avoid credentials, passwords, and personal data; the server does not classify sensitive content.",
              "maxLength": 256,
              "type": "string"
            }
          },
          "required": [
            "context"
          ],
          "type": "object"
        }
      },
      "required": [
        "telemetry",
        "org_slug",
        "pipeline_slug"
      ],
      "type": "object"
    }
  },
  {
    "name": "list_pipelines",
    "description": "List all pipelines in an organization with their basic details, build counts, and current status",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "detail_level": {
          "description": "Response detail level: 'summary' (default), 'detailed', or 'full'",
          "type": "string"
        },
        "name": {
          "description": "Filter pipelines by name",
          "type": "string"
        },
        "org_slug": {
          "type": "string"
        },
        "page": {
          "description": "Page number for pagination (min 1)",
          "type": "integer"
        },
        "per_page": {
          "description": "Results per page for pagination (min 1, max 100)",
          "type": "integer"
        },
        "repository": {
          "description": "Filter pipelines by repository URL",
          "type": "string"
        },
        "telemetry": {
          "additionalProperties": false,
          "description": "Analytics metadata describing the tool call's purpose",
          "properties": {
            "context": {
              "description": "Explain why calling this tool fits the user's overall goal. This parameter supports analytics and user-intent tracking. Provide 15-25 meaningful words in third-person perspective. Avoid credentials, passwords, and personal data; the server does not classify sensitive content.",
              "maxLength": 256,
              "type": "string"
            }
          },
          "required": [
            "context"
          ],
          "type": "object"
        }
      },
      "required": [
        "telemetry",
        "org_slug"
      ],
      "type": "object"
    }
  },
  {
    "name": "list_skills",
    "description": "List available skill guides that document usage patterns, pitfalls, and workflows for Buildkite MCP tools",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "query": {
          "description": "Optional case-insensitive filter over skill name and description",
          "type": "string"
        },
        "telemetry": {
          "additionalProperties": false,
          "description": "Analytics metadata describing the tool call's purpose",
          "properties": {
            "context": {
              "description": "Explain why calling this tool fits the user's overall goal. This parameter supports analytics and user-intent tracking. Provide 15-25 meaningful words in third-person perspective. Avoid credentials, passwords, and personal data; the server does not classify sensitive content.",
              "maxLength": 256,
              "type": "string"
            }
          },
          "required": [
            "context"
          ],
          "type": "object"
        }
      },
      "required": [
        "telemetry"
      ],
      "type": "object"
    }
  },
  {
    "name": "list_step_uploads",
    "description": "List the dynamic pipeline uploads (`buildkite-agent pipeline upload`) a build received, newest first, without their definitions. Each item includes state, the uploading job (source_job_id), created_jobs_count (omitted until applied, 0 when applied but no jobs were created), and rejection details. Use get_step_upload to read an upload's pipeline definition. Only available while the build is within its maximum lifetime (~30 days); older builds return 410 Gone",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "after": {
          "description": "Cursor from a previous response's links.next to fetch the next page",
          "type": "string"
        },
        "before": {
          "description": "Cursor from a previous response's links.prev to fetch the previous page",
          "type": "string"
        },
        "build_number": {
          "type": "string"
        },
        "org_slug": {
          "type": "string"
        },
        "per_page": {
          "description": "Results per page for pagination (min 1, max 100)",
          "type": "integer"
        },
        "pipeline_slug": {
          "type": "string"
        },
        "source_job_id": {
          "description": "Only return uploads performed by this job (a job UUID)",
          "type": "string"
        },
        "telemetry": {
          "additionalProperties": false,
          "description": "Analytics metadata describing the tool call's purpose",
          "properties": {
            "context": {
              "description": "Explain why calling this tool fits the user's overall goal. This parameter supports analytics and user-intent tracking. Provide 15-25 meaningful words in third-person perspective. Avoid credentials, passwords, and personal data; the server does not classify sensitive content.",
              "maxLength": 256,
              "type": "string"
            }
          },
          "required": [
            "context"
          ],
          "type": "object"
        }
      },
      "required": [
        "telemetry",
        "org_slug",
        "pipeline_slug",
        "build_number"
      ],
      "type": "object"
    }
  },
  {
    "name": "list_test_runs",
    "description": "List all test runs for a test suite in Buildkite Test Engine",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "org_slug": {
          "type": "string"
        },
        "page": {
          "description": "Page number for pagination (min 1)",
          "type": "integer"
        },
        "per_page": {
          "description": "Results per page for pagination (min 1, max 100)",
          "type": "integer"
        },
        "telemetry": {
          "additionalProperties": false,
          "description": "Analytics metadata describing the tool call's purpose",
          "properties": {
            "context": {
              "description": "Explain why calling this tool fits the user's overall goal. This parameter supports analytics and user-intent tracking. Provide 15-25 meaningful words in third-person perspective. Avoid credentials, passwords, and personal data; the server does not classify sensitive content.",
              "maxLength": 256,
              "type": "string"
            }
          },
          "required": [
            "context"
          ],
          "type": "object"
        },
        "test_suite_slug": {
          "type": "string"
        }
      },
      "required": [
        "telemetry",
        "org_slug",
        "test_suite_slug"
      ],
      "type": "object"
    }
  },
  {
    "name": "list_test_suites_for_pipeline",
    "description": "List the Buildkite Test Engine suites that have recorded at least one run attributed to a pipeline, ordered by creation time. Use this to find the test_suite_slug needed by the other Test Engine tools.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "org_slug": {
          "type": "string"
        },
        "page": {
          "description": "Page number for pagination (min 1)",
          "type": "integer"
        },
        "per_page": {
          "description": "Results per page for pagination (min 1, max 100)",
          "type": "integer"
        },
        "pipeline_slug": {
          "type": "string"
        },
        "telemetry": {
          "additionalProperties": false,
          "description": "Analytics metadata describing the tool call's purpose",
          "properties": {
            "context": {
              "description": "Explain why calling this tool fits the user's overall goal. This parameter supports analytics and user-intent tracking. Provide 15-25 meaningful words in third-person perspective. Avoid credentials, passwords, and personal data; the server does not classify sensitive content.",
              "maxLength": 256,
              "type": "string"
            }
          },
          "required": [
            "context"
          ],
          "type": "object"
        }
      },
      "required": [
        "telemetry",
        "org_slug",
        "pipeline_slug"
      ],
      "type": "object"
    }
  },
  {
    "name": "list_tests",
    "description": "List tests in a Buildkite Test Engine suite with execution metrics aggregated over a selected time window. Supports filtering, metric sorting, and pagination.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "branch": {
          "description": "Filter executions included in the metrics by branch. Prefix with '!' to exclude an exact branch or suffix with '*' to match by prefix, for example '!main' or 'feature*'. Use at most one operator.",
          "type": "string"
        },
        "labels": {
          "description": "Filter by comma-separated test labels. Prefix a label with '!' to exclude it, for example 'flaky,!slow'.",
          "type": "string"
        },
        "max_timestamp": {
          "description": "End of the aggregation window in RFC3339 format. Defaults to the current time when omitted.",
          "type": "string"
        },
        "min_executions": {
          "description": "Return only tests with at least this many executions after applying the selected time window and execution filters (min 1).",
          "type": "integer"
        },
        "min_timestamp": {
          "description": "Start of the aggregation window in RFC3339 format. When omitted, defaults to the organization's default Test Engine period before the current time.",
          "type": "string"
        },
        "order": {
          "description": "Sort direction: 'asc' or 'desc'. Defaults to 'desc'.",
          "type": "string"
        },
        "org_slug": {
          "type": "string"
        },
        "owners": {
          "description": "Filter by comma-separated test owner slugs. Prefix an owner with '!' to exclude it, for example 'payments,!platform'.",
          "type": "string"
        },
        "page": {
          "description": "Page number for pagination (min 1)",
          "type": "integer"
        },
        "per_page": {
          "description": "Results per page for pagination (min 1, max 100)",
          "type": "integer"
        },
        "period": {
          "description": "Relative aggregation window, such as '7days' or '28days'. Whether selected by period or timestamps, the aggregation window cannot exceed the organization's maximum Test Engine window. Cannot be combined with min_timestamp or max_timestamp.",
          "type": "string"
        },
        "sort_by": {
          "description": "Metric used to sort results: 'duration_avg', 'duration_sum', 'duration_min', 'duration_max', or 'reliability'. Defaults to 'duration_avg'. Metrics cover only executions in the selected window.",
          "type": "string"
        },
        "state": {
          "description": "Filter by test state: 'enabled', 'muted', or 'skipped'.",
          "type": "string"
        },
        "tags": {
          "description": "Filter by comma-separated execution tags in key:value form. Values support '!' for exclusion and '*' for prefix matching. Result values additionally support '~' for any matching execution and '^' for every execution matching, for example 'framework:!rspec,scm.branch:feature*,result:^passed'.",
          "type": "string"
        },
        "telemetry": {
          "additionalProperties": false,
          "description": "Analytics metadata describing the tool call's purpose",
          "properties": {
            "context": {
              "description": "Explain why calling this tool fits the user's overall goal. This parameter supports analytics and user-intent tracking. Provide 15-25 meaningful words in third-person perspective. Avoid credentials, passwords, and personal data; the server does not classify sensitive content.",
              "maxLength": 256,
              "type": "string"
            }
          },
          "required": [
            "context"
          ],
          "type": "object"
        },
        "test_suite_slug": {
          "type": "string"
        }
      },
      "required": [
        "telemetry",
        "org_slug",
        "test_suite_slug"
      ],
      "type": "object"
    }
  },
  {
    "name": "list_tests_for_build",
    "description": "List tests and their execution metrics for a Buildkite build. Requires the build UUID (the build ID, not the pipeline build number) and supports filtering, metric sorting, and pagination.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "branch": {
          "description": "Filter executions included in the metrics by branch. Prefix with '!' to exclude an exact branch or suffix with '*' for prefix matching, for example '!main' or 'feature*'. Use at most one operator.",
          "type": "string"
        },
        "build_uuid": {
          "description": "Buildkite build UUID. This is the build ID, not the pipeline build number.",
          "type": "string"
        },
        "labels": {
          "description": "Filter by comma-separated test labels. Prefix a label with '!' to exclude it, for example 'flaky,!slow'.",
          "type": "string"
        },
        "order": {
          "description": "Sort direction: 'asc' or 'desc'. Defaults to 'desc'.",
          "type": "string"
        },
        "org_slug": {
          "type": "string"
        },
        "owners": {
          "description": "Filter by comma-separated test owner slugs. Prefix an owner with '!' to exclude it, for example 'payments,!platform'.",
          "type": "string"
        },
        "page": {
          "description": "Page number for pagination (min 1)",
          "type": "integer"
        },
        "per_page": {
          "description": "Results per page for pagination (min 1, max 100)",
          "type": "integer"
        },
        "sort_by": {
          "description": "Metric used to sort results: 'duration_avg', 'duration_sum', 'duration_min', 'duration_max', or 'reliability'. Defaults to 'duration_avg'.",
          "type": "string"
        },
        "state": {
          "description": "Filter by test state: 'enabled', 'muted', or 'skipped'.",
          "type": "string"
        },
        "tags": {
          "description": "Filter by comma-separated execution tags in key:value form. Values support '!' for exclusion and '*' for prefix matching. Result values additionally support '~' for any matching execution and '^' for every execution matching, for example 'framework:!rspec,scm.branch:feature*,result:^passed'.",
          "type": "string"
        },
        "telemetry": {
          "additionalProperties": false,
          "description": "Analytics metadata describing the tool call's purpose",
          "properties": {
            "context": {
              "description": "Explain why calling this tool fits the user's overall goal. This parameter supports analytics and user-intent tracking. Provide 15-25 meaningful words in third-person perspective. Avoid credentials, passwords, and personal data; the server does not classify sensitive content.",
              "maxLength": 256,
              "type": "string"
            }
          },
          "required": [
            "context"
          ],
          "type": "object"
        }
      },
      "required": [
        "telemetry",
        "org_slug",
        "build_uuid"
      ],
      "type": "object"
    }
  },
  {
    "name": "load_skill",
    "description": "Load the full content of a skill guide by name",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "skill_name": {
          "description": "Name of the skill to load, as returned by list_skills",
          "type": "string"
        },
        "telemetry": {
          "additionalProperties": false,
          "description": "Analytics metadata describing the tool call's purpose",
          "properties": {
            "context": {
              "description": "Explain why calling this tool fits the user's overall goal. This parameter supports analytics and user-intent tracking. Provide 15-25 meaningful words in third-person perspective. Avoid credentials, passwords, and personal data; the server does not classify sensitive content.",
              "maxLength": 256,
              "type": "string"
            }
          },
          "required": [
            "context"
          ],
          "type": "object"
        }
      },
      "required": [
        "telemetry",
        "skill_name"
      ],
      "type": "object"
    }
  },
  {
    "name": "pause_cluster_queue_dispatch",
    "description": "Pause dispatch on a cluster queue, preventing new jobs from being dispatched to agents",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "cluster_id": {
          "type": "string"
        },
        "note": {
          "description": "Reason for pausing dispatch",
          "type": "string"
        },
        "org_slug": {
          "type": "string"
        },
        "queue_id": {
          "type": "string"
        },
        "telemetry": {
          "additionalProperties": false,
          "description": "Analytics metadata describing the tool call's purpose",
          "properties": {
            "context": {
              "description": "Explain why calling this tool fits the user's overall goal. This parameter supports analytics and user-intent tracking. Provide 15-25 meaningful words in third-person perspective. Avoid credentials, passwords, and personal data; the server does not classify sensitive content.",
              "maxLength": 256,
              "type": "string"
            }
          },
          "required": [
            "context"
          ],
          "type": "object"
        }
      },
      "required": [
        "telemetry",
        "org_slug",
        "cluster_id",
        "queue_id"
      ],
      "type": "object"
    }
  },
  {
    "name": "read_logs",
    "description": "Read log entries from the file, optionally starting from a specific row number. ALWAYS use 'limit' parameter to avoid excessive tokens. For recent failures, use 'tail_logs' instead. Recommended limits: investigation (100-500), exploration (use seek + small limits). The json format: {ts: timestamp_ms, c: content, rn: row_number}.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "build_number": {
          "type": "string"
        },
        "cache_ttl": {
          "type": "string"
        },
        "force_refresh": {
          "type": "boolean"
        },
        "job_id": {
          "type": "string"
        },
        "limit": {
          "type": "integer"
        },
        "org_slug": {
          "type": "string"
        },
        "pipeline_slug": {
          "type": "string"
        },
        "seek": {
          "type": "integer"
        },
        "telemetry": {
          "additionalProperties": false,
          "description": "Analytics metadata describing the tool call's purpose",
          "properties": {
            "context": {
              "description": "Explain why calling this tool fits the user's overall goal. This parameter supports analytics and user-intent tracking. Provide 15-25 meaningful words in third-person perspective. Avoid credentials, passwords, and personal data; the server does not classify sensitive content.",
              "maxLength": 256,
              "type": "string"
            }
          },
          "required": [
            "context"
          ],
          "type": "object"
        }
      },
      "required": [
        "telemetry",
        "org_slug",
        "pipeline_slug",
        "build_number",
        "job_id"
      ],
      "type": "object"
    }
  },
  {
    "name": "rebuild_build",
    "description": "Rebuild/retry an entire build on a Buildkite pipeline",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "build_number": {
          "type": "string"
        },
        "org_slug": {
          "type": "string"
        },
        "pipeline_slug": {
          "type": "string"
        },
        "telemetry": {
          "additionalProperties": false,
          "description": "Analytics metadata describing the tool call's purpose",
          "properties": {
            "context": {
              "description": "Explain why calling this tool fits the user's overall goal. This parameter supports analytics and user-intent tracking. Provide 15-25 meaningful words in third-person perspective. Avoid credentials, passwords, and personal data; the server does not classify sensitive content.",
              "maxLength": 256,
              "type": "string"
            }
          },
          "required": [
            "context"
          ],
          "type": "object"
        }
      },
      "required": [
        "telemetry",
        "org_slug",
        "pipeline_slug",
        "build_number"
      ],
      "type": "object"
    }
  },
  {
    "name": "resume_cluster_queue_dispatch",
    "description": "Resume dispatch on a paused cluster queue, allowing jobs to be dispatched to agents again",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "cluster_id": {
          "type": "string"
        },
        "org_slug": {
          "type": "string"
        },
        "queue_id": {
          "type": "string"
        },
        "telemetry": {
          "additionalProperties": false,
          "description": "Analytics metadata describing the tool call's purpose",
          "properties": {
            "context": {
              "description": "Explain why calling this tool fits the user's overall goal. This parameter supports analytics and user-intent tracking. Provide 15-25 meaningful words in third-person perspective. Avoid credentials, passwords, and personal data; the server does not classify sensitive content.",
              "maxLength": 256,
              "type": "string"
            }
          },
          "required": [
            "context"
          ],
          "type": "object"
        }
      },
      "required": [
        "telemetry",
        "org_slug",
        "cluster_id",
        "queue_id"
      ],
      "type": "object"
    }
  },
  {
    "name": "retry_job",
    "description": "Retry a specific failed or timed out job in a Buildkite build",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "build_number": {
          "type": "string"
        },
        "job_id": {
          "type": "string"
        },
        "org_slug": {
          "type": "string"
        },
        "pipeline_slug": {
          "type": "string"
        },
        "telemetry": {
          "additionalProperties": false,
          "description": "Analytics metadata describing the tool call's purpose",
          "properties": {
            "context": {
              "description": "Explain why calling this tool fits the user's overall goal. This parameter supports analytics and user-intent tracking. Provide 15-25 meaningful words in third-person perspective. Avoid credentials, passwords, and personal data; the server does not classify sensitive content.",
              "maxLength": 256,
              "type": "string"
            }
          },
          "required": [
            "context"
          ],
          "type": "object"
        }
      },
      "required": [
        "telemetry",
        "org_slug",
        "pipeline_slug",
        "build_number",
        "job_id"
      ],
      "type": "object"
    }
  },
  {
    "name": "search_logs",
    "description": "Search log entries using regex patterns with optional context lines. For recent failures, try 'tail_logs' first, then use search_logs with patterns like 'error|failed|exception' and limit: 10-20. The json format: {ts: timestamp_ms, c: content, rn: row_number}.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "after_context": {
          "type": "integer"
        },
        "before_context": {
          "type": "integer"
        },
        "build_number": {
          "type": "string"
        },
        "cache_ttl": {
          "type": "string"
        },
        "case_sensitive": {
          "type": "boolean"
        },
        "context": {
          "type": "integer"
        },
        "force_refresh": {
          "type": "boolean"
        },
        "invert_match": {
          "type": "boolean"
        },
        "job_id": {
          "type": "string"
        },
        "limit": {
          "type": "integer"
        },
        "org_slug": {
          "type": "string"
        },
        "pattern": {
          "type": "string"
        },
        "pipeline_slug": {
          "type": "string"
        },
        "reverse": {
          "type": "boolean"
        },
        "seek_start": {
          "type": "integer"
        },
        "telemetry": {
          "additionalProperties": false,
          "description": "Analytics metadata describing the tool call's purpose",
          "properties": {
            "context": {
              "description": "Explain why calling this tool fits the user's overall goal. This parameter supports analytics and user-intent tracking. Provide 15-25 meaningful words in third-person perspective. Avoid credentials, passwords, and personal data; the server does not classify sensitive content.",
              "maxLength": 256,
              "type": "string"
            }
          },
          "required": [
            "context"
          ],
          "type": "object"
        }
      },
      "required": [
        "telemetry",
        "org_slug",
        "pipeline_slug",
        "build_number",
        "job_id",
        "pattern"
      ],
      "type": "object"
    }
  },
  {
    "name": "tail_logs",
    "description": "Show the last N entries from the log file. RECOMMENDED for failure diagnosis - most build failures appear in the final log entries. More token-efficient than read_logs for recent issues. The json format: {ts: timestamp_ms, c: content, rn: row_number}.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "build_number": {
          "type": "string"
        },
        "cache_ttl": {
          "type": "string"
        },
        "force_refresh": {
          "type": "boolean"
        },
        "job_id": {
          "type": "string"
        },
        "org_slug": {
          "type": "string"
        },
        "pipeline_slug": {
          "type": "string"
        },
        "tail": {
          "type": "integer"
        },
        "telemetry": {
          "additionalProperties": false,
          "description": "Analytics metadata describing the tool call's purpose",
          "properties": {
            "context": {
              "description": "Explain why calling this tool fits the user's overall goal. This parameter supports analytics and user-intent tracking. Provide 15-25 meaningful words in third-person perspective. Avoid credentials, passwords, and personal data; the server does not classify sensitive content.",
              "maxLength": 256,
              "type": "string"
            }
          },
          "required": [
            "context"
          ],
          "type": "object"
        }
      },
      "required": [
        "telemetry",
        "org_slug",
        "pipeline_slug",
        "build_number",
        "job_id"
      ],
      "type": "object"
    }
  },
  {
    "name": "unblock_job",
    "description": "Unblock a blocked job in a Buildkite build to allow it to continue execution",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "build_number": {
          "type": "string"
        },
        "fields": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "JSON object containing string values for block step fields",
          "type": "object"
        },
        "job_id": {
          "type": "string"
        },
        "org_slug": {
          "type": "string"
        },
        "pipeline_slug": {
          "type": "string"
        },
        "telemetry": {
          "additionalProperties": false,
          "description": "Analytics metadata describing the tool call's purpose",
          "properties": {
            "context": {
              "description": "Explain why calling this tool fits the user's overall goal. This parameter supports analytics and user-intent tracking. Provide 15-25 meaningful words in third-person perspective. Avoid credentials, passwords, and personal data; the server does not classify sensitive content.",
              "maxLength": 256,
              "type": "string"
            }
          },
          "required": [
            "context"
          ],
          "type": "object"
        }
      },
      "required": [
        "telemetry",
        "org_slug",
        "pipeline_slug",
        "build_number",
        "job_id"
      ],
      "type": "object"
    }
  },
  {
    "name": "update_cluster",
    "description": "Update an existing cluster's name, description, emoji, color, or default queue",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "cluster_id": {
          "type": "string"
        },
        "color": {
          "description": "New hex color code for the cluster",
          "type": [
            "null",
            "string"
          ]
        },
        "default_queue_id": {
          "description": "ID of the default queue for the cluster",
          "type": [
            "null",
            "string"
          ]
        },
        "description": {
          "description": "New description for the cluster",
          "type": [
            "null",
            "string"
          ]
        },
        "emoji": {
          "description": "New emoji for the cluster",
          "type": [
            "null",
            "string"
          ]
        },
        "name": {
          "description": "New name for the cluster",
          "type": [
            "null",
            "string"
          ]
        },
        "org_slug": {
          "type": "string"
        },
        "telemetry": {
          "additionalProperties": false,
          "description": "Analytics metadata describing the tool call's purpose",
          "properties": {
            "context": {
              "description": "Explain why calling this tool fits the user's overall goal. This parameter supports analytics and user-intent tracking. Provide 15-25 meaningful words in third-person perspective. Avoid credentials, passwords, and personal data; the server does not classify sensitive content.",
              "maxLength": 256,
              "type": "string"
            }
          },
          "required": [
            "context"
          ],
          "type": "object"
        }
      },
      "required": [
        "telemetry",
        "org_slug",
        "cluster_id"
      ],
      "type": "object"
    }
  },
  {
    "name": "update_cluster_queue",
    "description": "Update an existing cluster queue's description or retry agent affinity",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "cluster_id": {
          "type": "string"
        },
        "description": {
          "description": "New description for the queue",
          "type": [
            "null",
            "string"
          ]
        },
        "org_slug": {
          "type": "string"
        },
        "queue_id": {
          "type": "string"
        },
        "retry_agent_affinity": {
          "description": "Agent retry affinity: prefer-warmest or prefer-different",
          "type": [
            "null",
            "string"
          ]
        },
        "telemetry": {
          "additionalProperties": false,
          "description": "Analytics metadata describing the tool call's purpose",
          "properties": {
            "context": {
              "description": "Explain why calling this tool fits the user's overall goal. This parameter supports analytics and user-intent tracking. Provide 15-25 meaningful words in third-person perspective. Avoid credentials, passwords, and personal data; the server does not classify sensitive content.",
              "maxLength": 256,
              "type": "string"
            }
          },
          "required": [
            "context"
          ],
          "type": "object"
        }
      },
      "required": [
        "telemetry",
        "org_slug",
        "cluster_id",
        "queue_id"
      ],
      "type": "object"
    }
  },
  {
    "name": "update_pipeline",
    "description": "Modify an existing Buildkite pipeline's configuration, repository, settings, or metadata",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "cancel_running_branch_builds": {
          "description": "Cancel running builds when new builds are created on the same branch",
          "type": [
            "null",
            "boolean"
          ]
        },
        "cluster_id": {
          "type": [
            "null",
            "string"
          ]
        },
        "configuration": {
          "description": "The pipeline configuration in YAML format",
          "type": [
            "null",
            "string"
          ]
        },
        "default_branch": {
          "description": "The default branch for builds and metrics filtering",
          "type": [
            "null",
            "string"
          ]
        },
        "description": {
          "type": [
            "null",
            "string"
          ]
        },
        "name": {
          "type": [
            "null",
            "string"
          ]
        },
        "org_slug": {
          "type": "string"
        },
        "pipeline_slug": {
          "type": "string"
        },
        "repository_url": {
          "description": "The Git repository URL",
          "type": [
            "null",
            "string"
          ]
        },
        "skip_queued_branch_builds": {
          "description": "Skip intermediate builds when new builds are created on the same branch",
          "type": [
            "null",
            "boolean"
          ]
        },
        "tags": {
          "description": "Tags to apply to the pipeline for filtering and organization",
          "items": {
            "type": "string"
          },
          "type": [
            "null",
            "array"
          ]
        },
        "telemetry": {
          "additionalProperties": false,
          "description": "Analytics metadata describing the tool call's purpose",
          "properties": {
            "context": {
              "description": "Explain why calling this tool fits the user's overall goal. This parameter supports analytics and user-intent tracking. Provide 15-25 meaningful words in third-person perspective. Avoid credentials, passwords, and personal data; the server does not classify sensitive content.",
              "maxLength": 256,
              "type": "string"
            }
          },
          "required": [
            "context"
          ],
          "type": "object"
        }
      },
      "required": [
        "telemetry",
        "org_slug",
        "pipeline_slug"
      ],
      "type": "object"
    }
  },
  {
    "name": "update_pipeline_schedule",
    "description": "Modify an existing pipeline schedule's cron expression, branch, environment variables, or enabled state",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "branch": {
          "type": [
            "null",
            "string"
          ]
        },
        "commit": {
          "type": [
            "null",
            "string"
          ]
        },
        "cronline": {
          "description": "Schedule interval as a crontab expression or predefined value",
          "type": [
            "null",
            "string"
          ]
        },
        "enabled": {
          "description": "Whether the schedule is active. Re-enabling clears previous failure data.",
          "type": [
            "null",
            "boolean"
          ]
        },
        "env": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "Environment variables to set on triggered builds. Providing this field REPLACES the existing env map entirely — include all keys you want to retain.",
          "type": "object"
        },
        "label": {
          "type": [
            "null",
            "string"
          ]
        },
        "message": {
          "type": [
            "null",
            "string"
          ]
        },
        "org_slug": {
          "type": "string"
        },
        "pipeline_slug": {
          "type": "string"
        },
        "schedule_id": {
          "type": "string"
        },
        "telemetry": {
          "additionalProperties": false,
          "description": "Analytics metadata describing the tool call's purpose",
          "properties": {
            "context": {
              "description": "Explain why calling this tool fits the user's overall goal. This parameter supports analytics and user-intent tracking. Provide 15-25 meaningful words in third-person perspective. Avoid credentials, passwords, and personal data; the server does not classify sensitive content.",
              "maxLength": 256,
              "type": "string"
            }
          },
          "required": [
            "context"
          ],
          "type": "object"
        }
      },
      "required": [
        "telemetry",
        "org_slug",
        "pipeline_slug",
        "schedule_id"
      ],
      "type": "object"
    }
  },
  {
    "name": "user_token_organization",
    "description": "Get the organization associated with the user token used for this request",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "telemetry": {
          "additionalProperties": false,
          "description": "Analytics metadata describing the tool call's purpose",
          "properties": {
            "context": {
              "description": "Explain why calling this tool fits the user's overall goal. This parameter supports analytics and user-intent tracking. Provide 15-25 meaningful words in third-person perspective. Avoid credentials, passwords, and personal data; the server does not classify sensitive content.",
              "maxLength": 256,
              "type": "string"
            }
          },
          "required": [
            "context"
          ],
          "type": "object"
        }
      },
      "required": [
        "telemetry"
      ],
      "type": "object"
    }
  },
  {
    "name": "wait_for_build",
    "description": "Wait for a build to reach a terminal state (passed, failed, canceled, skipped, not_run, or blocked on a block step), polling for up to 45 seconds. Returns finished=true along with the build once it settles. If the build is still in progress when the window closes it returns finished=false and the current state only, with no build detail — call this tool again to keep waiting. Judge a long build by build_elapsed_seconds, which counts from the build's own start time and does not reset between calls; waited_seconds covers only the latest call. Do not wait indefinitely: after roughly ten consecutive calls, stop and report the build as still running with its elapsed time rather than continuing to poll. Jobs and annotation bodies are never included — use list_jobs or get_job for job detail, and list_annotations to read annotations",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "build_number": {
          "type": "string"
        },
        "org_slug": {
          "type": "string"
        },
        "pipeline_slug": {
          "type": "string"
        },
        "telemetry": {
          "additionalProperties": false,
          "description": "Analytics metadata describing the tool call's purpose",
          "properties": {
            "context": {
              "description": "Explain why calling this tool fits the user's overall goal. This parameter supports analytics and user-intent tracking. Provide 15-25 meaningful words in third-person perspective. Avoid credentials, passwords, and personal data; the server does not classify sensitive content.",
              "maxLength": 256,
              "type": "string"
            }
          },
          "required": [
            "context"
          ],
          "type": "object"
        }
      },
      "required": [
        "telemetry",
        "org_slug",
        "pipeline_slug",
        "build_number"
      ],
      "type": "object"
    }
  }
]
